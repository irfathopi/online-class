import React from 'react';

const Cart = (props) => {
    // const price = props.Courses.price;
    return (
        <div>
            <h2>This is a cart : </h2>
        </div>
    );
};

export default Cart;